/**
 * The {@code inventory} module. Owns its own tables and exposes a service API to other modules.
 * Other modules must not reach into this package's repositories or entities.
 */
package in.nirman.modules.inventory;
